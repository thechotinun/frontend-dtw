

export const metadata = {
  title: "Login-dtw",
};

export default function LoginLayout({ children }) {
  return (
    <div>
      <nav>LoginLayout</nav>
      <main>{children}</main>
    </div>
  );
}
